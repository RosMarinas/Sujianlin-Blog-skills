---
type: proposition
title: 自回归与非自回归的曝光偏差差异
aliases: []
statement: 在序列生成任务中，自回归模型由于训练时使用 Teacher Forcing（输入真实历史）而推理时使用前一步的模型预测，容易在推理阶段产生误差累积（曝光偏差）；而非自回归模型（如基于 MLM 的问答）由于在训练和推理时输入均不依赖前步自回归预测，不存在曝光偏差问题。
assumptions: ["自回归模型采用自回归逐词生成概率公式", "非自回归/MLM 模型并行生成或填充掩码"]
sources:
  - Data/Spaces_ac_cn/markdown/Big-Data/2019-12-26-非自回归-也不差-基于MLM的阅读理解问答.md
source_ids:
  - '7148'
proof_route: |
  1. 形式化自回归模型条件概率建模为 $P(Y|X) = \prod_{t=1}^T P(y_t | y_{<t}, X)$。训练时 $y_{<t}$ 来自 ground truth，推理时 $y_{<t}$ 来自模型采样，这导致两者的分布差异，造成误差扩散。
  2. 形式化非自回归/MLM 模型，预测时将目标位置全部替换为 `[MASK]`，训练和推理时的输入均不依赖生成的 token 历史，这保证了输入特征的一致性，从根本上消除了自回归所独有的曝光偏差风险。
evidence_spans: ["ev::7148::非自回归"]
status: draft
updated: 2026-06-11
---

# 自回归与非自回归的曝光偏差差异

该命题解释了非自回归/MLM 模型用于短文本抽取式问答时，在精度与自回归持平的同时能获得大幅提顺且不易产生幻觉的理论依据。
