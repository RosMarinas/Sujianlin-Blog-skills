---
type: proposition
title: 残差门卷积的流式等价性
aliases: []
statement: 残差门卷积结构 \boldsymbol{Y}=\boldsymbol{X} + \text{Conv1D}_1(\boldsymbol{X}) \otimes \sigma\Big(\text{Conv1D}_2(\boldsymbol{X})\Big) 与 GRU-like 信息流模型 \boldsymbol{Y}=\boldsymbol{X}\otimes \Big(1-\boldsymbol{\sigma}\Big) + \text{Conv1D}_1'(\boldsymbol{X}) \otimes \boldsymbol{\sigma} 在参数线性表示意义上完全等价。
assumptions: ["Conv1D_1 为无激活的线性一维卷积", "σ 为 Sigmoid 门控通道"]
sources:
  - Data/Spaces_ac_cn/markdown/Big-Data/2018-04-15-基于CNN的阅读理解式问答模型-DGCNN.md
source_ids:
  - '5409'
proof_route: |
  1. 将 GRU-like 表达式展开：$\boldsymbol{Y} = \boldsymbol{X} \otimes (1 - \boldsymbol{\sigma}) + \text{Conv1D}_1'(\boldsymbol{X}) \otimes \boldsymbol{\sigma} = \boldsymbol{X} + (\text{Conv1D}_1'(\boldsymbol{X}) - \boldsymbol{X}) \otimes \boldsymbol{\sigma}$。
  2. 由于 $\text{Conv1D}_1'$ 为不加激活函数的线性卷积操作，$\text{Conv1D}_1'(\boldsymbol{X}) - \boldsymbol{X}$ 仍属于对 $\boldsymbol{X}$ 的线性变换空间，可等效由单个线性一维卷积 $\text{Conv1D}_1(\boldsymbol{X})$ 参数化表示。
  3. 从而两种表达在模型容量和优化流向上是等价的。
evidence_spans: ["ev::5409::残差门卷积"]
status: draft
updated: 2026-06-11
---

# 残差门卷积的流式等价性

此命题从参数表达的角度论证了为什么简单的门卷积残差形式能够取得与复杂门控 GRU/LSTM 结构相媲美的高质量信息流传输。
