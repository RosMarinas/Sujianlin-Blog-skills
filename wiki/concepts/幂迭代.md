---
type: concept
title: 幂迭代
aliases:
- Power iteration
- Power method
definition: 迭代求解矩阵最大特征值和特征向量的方法：u <- (W^T W)u / ||(W^T W)u||
standard_notation: u <- (W^T W)u / ||(W^T W)u||
sources:
- Data/Spaces_ac_cn/markdown/Big-Data/2018-10-07-深度学习中的Lipschitz约束-泛化与生成模型.md
source_ids:
- '6051'
related_methods:
- - 幂迭代求谱范数
evidence_spans: []
series: []
status: draft
updated: '2026-06-12'
---
## 定义

迭代求解矩阵最大特征值和特征向量的方法：u <- (W^T W)u / ||(W^T W)u||

## 标准符号

u <- (W^T W)u / ||(W^T W)u||

记输入为$x$，输出为$y$，模型为$f$，模型参数为$w$，记为

$$

y = f_w(x)

事实上，前面的内容虽然看起来茫然，但却是求$\Vert W\Vert_2$的基础。前一节告诉我们$\Vert W\Vert_2^2$就是$W^{\top}W$的最大特征根，所以问题变成了求$W^{\top}W$的最大特征根，这可以通过[“幂迭代”法](https://en.wikipedia.org/wiki/Power_iteration)来解决。
