---
type: concept
title: 多尺度损失
aliases:
- Multi-Scale Loss
- 多尺度Loss
definition: 一种扩散模型训练损失，通过对噪声和目标分别在多个分辨率尺度上计算MSE损失并取平均，使模型在多个空间尺度上同时学习去噪，提升高分辨率图像生成的训练稳定性和效果。
sources:
- Data/Spaces_ac_cn/markdown/Big-Data/2024-04-08-生成扩散模型漫谈-二十二-信噪比与大图生成-上.md
source_ids:
- '10047'
prerequisites:
- 前向扩散过程
- 噪声预测网络
related_methods: []
evidence_spans:
- 10047-架构拓展
status: draft
updated: '2026-06-12'
---

## 定义

多尺度损失对标准扩散MSE损失进行泛化。标准损失在原始分辨率上计算预测噪声与实际噪声的均方误差：
$$\mathcal{L} = \frac{1}{wh}\|\boldsymbol{\varepsilon} - \boldsymbol{\epsilon}_{\boldsymbol{\theta}}(\bar{\alpha}_t \boldsymbol{x}_0 + \bar{\beta}_t \boldsymbol{\varepsilon}, t)\|^2$$

多尺度损失引入下采样算子 $\mathcal{D}_{w/s \times h/s}[\cdot]$（平均Pooling），对噪声和目标同时下采样到 $w/s \times h/s$ 再计算MSE：
$$\mathcal{L}_{s \times s} = \frac{1}{(w/s)(h/s)}\|\mathcal{D}_{w/s \times h/s}[\boldsymbol{\varepsilon}] - \mathcal{D}_{w/s \times h/s}[\boldsymbol{\epsilon}_{\boldsymbol{\theta}}(\bar{\alpha}_t \boldsymbol{x}_0 + \bar{\beta}_t \boldsymbol{\varepsilon}, t)]\|^2$$

最终损失为多个 $s$ 对应的 $\mathcal{L}_{s \times s}$ 的平均。

## 性质

- 与SNR对齐调度迁移目的一致：保证高分辨率扩散模型至少在低分辨率维度上不低于直接训练的低分辨率模型。
- 迫使模型同时在多个空间尺度上学习去噪，有利于捕获跨尺度的图像结构。
- 计算成本增加可控，因为下采样后的分辨率更低。