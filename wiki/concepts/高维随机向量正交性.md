---

type: concept
title: 高维随机向量正交性
aliases: []
definition: 高维空间中任意两个独立同分布的随机向量几乎必然近似正交，内积 $\langle\boldsymbol{x},\boldsymbol{y}\rangle\approx
  0$。
standard_notation: false
sources:
- Data/Spaces_ac_cn/markdown/Mathematics/2020-01-16-从几何视角来理解模型参数的初始化策略.md
source_ids:
- '7180'
prerequisites: []
equivalent_forms: []
direct_consequences: []
related_formulas: []
related_methods: []
series: []
evidence_spans: []
status: draft
updated: '2026-06-12'
---
# 高维随机向量正交性

## 定义
高维随机向量正交性是指随着空间维度 $d \to \infty$，高维空间中两个独立同分布的零均值随机向量几乎必然近似正交的数学现象。

## 几何直觉
若 $\boldsymbol{x}, \boldsymbol{y}$ 均为 $d$ 维单位随机向量，其内积的均值为 0，方差正比于 $1/d$。当维度 $d$ 极大时，内积以极大概率落在零的附近。

前者时间笔者写了[《n维空间下两个随机向量的夹角分布》](/archives/7076)，其中的一个推论是

> 推论1： 高维空间中的任意两个随机向量几乎都是垂直的。

事实上，推论1正是本文整个几何视角的出发点！它的一个更进一步的推论是：

对于复杂模型来说，参数的初始化显得尤为重要。糟糕的初始化，很多时候已经不单是模型效果变差的问题了，还更有可能是模型根本训练不动或者不收敛。在深度学习中常见的自适应初始化策略是Xavier初始化，它是从正态分布$\mathcal{N}\left(0,\frac{2}{fan_{in} + fan_{out}}\right)$中随机采样而构成的初始权重，其中$fan_{in}$是输入的维度而$fan_{out}$是输出的维度。其他初始化策略基本上也类似，只不过假设有所不同，导致最终形式略有差别。

前者时间笔者写了[《n维空间下两个随机向量的夹角分布》](/archives/7076)，其中的一个推论是

> 推论2： 从$\mathcal{N}(0, 1/n)$中随机选取$n^2$个数，组成一个$n\times n$的矩阵，这个矩阵近似为正交矩阵，且$n$越大，近似程度越好。
