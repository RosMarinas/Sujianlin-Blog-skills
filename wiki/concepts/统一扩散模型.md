---
type: concept
title: 统一扩散模型
aliases:
- Unified Diffusion Model
- UDM
definition: 一种将扩散模型的前向过程抽象为任意确定性变换$\boldsymbol{x}_t = \boldsymbol{\mathcal{F}}_t(\boldsymbol{x}_0,\boldsymbol{\varepsilon})$、反向过程分解为"预估-修正"两步的元理论框架，可包含DDPM、DDIM、SDE和ODE为特例。
sources:
- Data/Spaces_ac_cn/markdown/Big-Data/2022-09-14-生成扩散模型漫谈-十-统一扩散模型-理论篇.md
source_ids:
- '9262'
prerequisites:
- '[[边缘一致性]]'
equivalent_forms: []
direct_consequences: []
related_formulas: []
related_methods:
- '[[估计-校正分解]]'
series:
- '[[生成扩散模型漫谈]]'
evidence_spans:
- ev::9262::思考分析
status: draft
updated: '2026-06-12'
---

UDM框架有四大目标：不限对象类型（连续/离散）、不限前向过程（加噪/模糊/遮掩/删减）、不限时间类型（离散/连续）、包含已有结果。前向过程直接定义$\boldsymbol{x}_t = \boldsymbol{\mathcal{F}}_t(\boldsymbol{x}_0,\boldsymbol{\varepsilon})$，其中$\boldsymbol{\mathcal{F}}_t$是渐进破坏变换。反向过程恒等式$p(\boldsymbol{x}_{t-1}|\boldsymbol{x}_t) = \int p(\boldsymbol{x}_{t-1}|\boldsymbol{x}_t,\boldsymbol{x}_0)p(\boldsymbol{x}_0|\boldsymbol{x}_t)d\boldsymbol{x}_0$将生成分解为"预估"（由$\boldsymbol{x}_t$估$\boldsymbol{x}_0$）和"修正"（由估计值推$\boldsymbol{x}_{t-1}$）。$p(\boldsymbol{x}_{t-1}|\boldsymbol{x}_t,\boldsymbol{x}_0)$的设计需满足边缘一致性约束。UDM的核心方法论贡献是区分了扩散模型中的"设计自由"与"数学约束"。