---
type: concept
title: 优化快慢动力学
aliases: []
definition: 把模型不同模块的更新速度区分为快变量和慢变量，用于解释退化或稳定现象。在无负样本的对比学习模型中，通过动力学分解防止模型退化为常数。
sources:
- Data/Spaces_ac_cn/markdown/Big-Data/2020-12-11-从动力学角度看优化算法-六-为什么SimSiam不退化.md
source_ids:
- '7980'
related_methods:
- - - 把优化算法解释为动力系统离散化
series:
- - - 从动力学角度看优化算法
evidence_spans:
- ev::7980::不退化的动力学
status: draft
updated: '2026-06-12'
---

# 优化快慢动力学

## 定义

把模型不同模块的更新速度区分为快变量和慢变量，用于解释退化或稳定现象。在无负样本的特征学习（如SimSiam、BYOL）中，模型没有依靠负样本来防止特征向量相近，而是通过将模型的优化过程分解为两个同步的、但是快慢不一样的模块来防止退化（坍缩）为一个没有意义的常数模型。

## 动力学分析与属性

在SimSiam等方法中，优化目标可以写为：
$$
\mathcal{L}(\varphi, \theta)=\mathbb{E}_{x, \mathcal{T}_1,\mathcal{T}_2}\Big[l\left(h_{\varphi}(f_{\theta}(\mathcal{T}_1(x))), f_{\theta}(\mathcal{T}_2(x))\right)\Big]
$$
其中 $h_{\varphi}$ 为 predictor 模块，$f_{\theta}$ 为 encoder 模块。用梯度下降优化时，对应的动力学方程组是：
$$
\begin{aligned}
\frac{d\varphi}{dt} = - \frac{\partial\mathcal{L}}{\partial \varphi} =& -\mathbb{E}_{x, \mathcal{T}_1,\mathcal{T}_2}\bigg[\frac{\partial l}{\partial h}\frac{\partial h}{\partial \varphi}\bigg]\\
\frac{d\theta}{dt} = - \frac{\partial\mathcal{L}}{\partial \theta} =& -\mathbb{E}_{x, \mathcal{T}_1,\mathcal{T}_2}\bigg[\frac{\partial l}{\partial h}\frac{\partial h}{\partial f}\frac{\partial f}{\partial \theta} \color{skyblue}{\,+\underbrace{\frac{\partial l}{\partial f}\frac{\partial f}{\partial \theta}}_{\begin{aligned}\text{SimSiam}\\\text{去掉了它}\end{aligned}}}\bigg]
\end{aligned}
$$

如果添加了 stop_gradient 算子，那么 $\frac{d\theta}{dt}$ 就少了上式蓝色标注的第二项。这时候 $\frac{d\varphi}{dt}$ 和 $\frac{d\theta}{dt}$ 都共同包含因子 $\frac{\partial l}{\partial h}$。
由于 $h_{\varphi}$ 更靠近输出层，开始学习的时候 $h_{\varphi}$ 会被优化得更快。也就是说，$\frac{d\varphi}{dt}$ 是快动力学部分，$\frac{d\theta}{dt}$ 则是慢动力学部分。相对而言，$\frac{d\varphi}{dt}$ 会更快速地收敛，这意味着 $\frac{\partial l}{\partial h}$ 会很快地变得很小。由于 $\frac{d\theta}{dt}$ 也包含这一项，所以在它退化之前，推动它退化的力就已经微乎其微了，也就不会退化了。
相反，如果保留第二项 $\frac{\partial l}{\partial f}\frac{\partial f}{\partial \theta}$，就相当于添加了一个“快速通道”，即使 $\frac{\partial l}{\partial h}=0$，由于第二项存在，还会继续推动模型退化。

## 具体例子与对比

以标量目标函数为例：
$$
l = \frac{1}{2}(\varphi\theta - \theta)^2
$$
其对应的动力学方程是：
$$
\frac{d\varphi}{dt}=-(\varphi\theta - \theta)\theta, \quad\frac{d\theta}{dt}=-(\varphi\theta - \theta) \varphi \color{skyblue}{+ \underbrace{(\varphi\theta - \theta)}_{\begin{aligned}\text{SimSiam}\theta\\ \text{去掉了它}\end{aligned}}}
$$
- **停掉第二个 $\theta$ 的梯度（存在 stop_gradient）**：$\varphi$ 和 $\theta$ 的方程相当一致，$\varphi$ 迅速趋于1，同时 $\theta$ 稳定到了一个非0值（意味着没退化）。
- **不停掉第二个 $\theta$ 的梯度（无 stop_gradient）**：$\theta$ 迅速趋于0，而 $\varphi$ 无法趋于1，主导权被 $\theta$ 占据并发生退化。

## 结论

predictor（$\varphi$）的引入使得模型的动力学分为了两大部分，stop_gradient算子的引入则使得encoder部分（$\theta$）的动力学变慢，并且增强了encoder与predictor的同步性，这样一来，predictor以“迅雷不及掩耳之势”拟合了目标，使得encoder还没来得及退化，优化过程就停止了。

## 在本批次中的作用

该概念用于把源文中的局部推导归入可查询的认知对象，并通过 evidence span 保留来源。

## 证据

- `ev::7980::不退化的动力学`
